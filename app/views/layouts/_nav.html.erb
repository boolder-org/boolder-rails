<nav class="bg-white shadow">
  <div class="max-w-7xl mx-auto px-2 sm:px-4 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex px-2 lg:px-0">
        
        <div class="flex-shrink-0 flex items-center">
          <%= link_to root_localized_path do %>
            <%= image_tag 'logo.svg', class: "h-8 w-auto", alt:"Boolder" %>
          <% end %>
        </div>
        <div class="ml-6 flex space-x-8">
          <!-- Current: "border-emerald-500 text-gray-900", Default: "border-transparent text-gray-500 hover:border-gray-300 hover:text-gray-700" -->
          <%= link_to areas_path, class: "flex items-center px-1 pt-1 font-medium text-gray-500 hover:text-gray-900" do %>
            <%= t("views.layout.nav.discover") %> 
          <% end %>
          
          <%= link_to map_path, class: "flex items-center px-1 pt-1 font-medium text-gray-500 hover:text-gray-900" do %>
            <%= t("views.layout.nav.map") %>
          <% end %>

          <%= render 'shared/search_modal', show_button: true %>
        </div>
      </div>

    </div>
  </div>

</nav>